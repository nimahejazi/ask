## Agent skills

### Issue tracker

Issues live as markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses five canonical roles (`needs-triage`, etc.). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (root CONTEXT.md + ADRs). See `docs/agents/domain.md`.
